# honeypot-behavior-analysis
To get a sense of session metadata collected from a honeypot with the goal of determining when a session is initiated by a human or when a session is initiated by a non-human.
